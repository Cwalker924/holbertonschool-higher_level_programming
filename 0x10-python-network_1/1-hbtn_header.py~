#!/usr/bin/python3
import urllib.request
import sys


if __name__ == "__main__":
    url = "$1"
    with urllib.request.urlopen(sys.url) as response:
        var = response.info()
        print(var.get('X-Request-Id'))
